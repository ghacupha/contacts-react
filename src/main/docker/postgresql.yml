version: '2'
services:
  contacts-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/contacts/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=contacts
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
